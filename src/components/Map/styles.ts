import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    width: "80%",
    height: '50%',
    backgroundColor: "red",
    borderColor: "black",
    borderRadius: 8,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 12
  },
});
