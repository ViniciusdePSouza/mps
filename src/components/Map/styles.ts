import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    width: "100%",
    minHeight: 700,
    alignItems: "center",
    justifyContent: "center",
    marginTop: 12,
  },

  map: {
    flex: 1,
    width: "100%",
    borderRadius: 8,
    marginBottom: 12,
  },

  banner: {
    marginTop: 12
  },

  title: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 12
  },

  instructions: {
    fontSize: 16,
    fontWeight: "normal",
    color: '#b7bbc0'
  }
});
